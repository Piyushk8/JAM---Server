name: Ping Backend

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping the backend
        run: curl -s https://jam-server-54gm.onrender.com/api/v1/user/me
